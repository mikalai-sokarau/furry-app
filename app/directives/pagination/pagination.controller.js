export default function($stateParams) {
    this.paginationData = [];
    this.currentPage = $stateParams.page;

    function fillPaginationArr() {
        const resultArr = [];

        if (this.currentPage )
    }
}
